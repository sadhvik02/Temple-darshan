import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors - Spiritual & Devotional Saffron / Gold
  static const Color primary = Color(0xFFD84315); // Deep Saffron Orange
  static const Color primaryDark = Color(0xFFBF360C);
  static const Color primaryLight = Color(0xFFFF8A65);
  static const Color accent = Color(0xFFFFA000); // Sacred Gold / Amber
  static const Color accentGold = Color(0xFFFFD54F);

  // Background & Surfaces
  static const Color background = Color(0xFFFBF9F5); // Warm calm sand
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F1E8);
  static const Color cardBorder = Color(0xFFEFE8DE);

  // Text Colors
  static const Color textPrimary = Color(0xFF1C1B1F);
  static const Color textSecondary = Color(0xFF5D5C5B);
  static const Color textTertiary = Color(0xFF8E8D8C);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status Colors
  static const Color statusConfirmed = Color(0xFF2E7D32); // Deep Green
  static const Color statusPending = Color(0xFFE65100); // Warm Orange
  static const Color statusCancelled = Color(0xFFC62828); // Deep Red
  static const Color statusCompleted = Color(0xFF1565C0); // Deep Blue

  // Status Background Tints
  static const Color statusConfirmedBg = Color(0xFFE8F5E9);
  static const Color statusPendingBg = Color(0xFFFFF3E0);
  static const Color statusCancelledBg = Color(0xFFFFEBEE);
  static const Color statusCompletedBg = Color(0xFFE3F2FD);

  // Utilities
  static const Color divider = Color(0xFFECE6DD);
  static const Color shimmerBase = Color(0xFFE0E0E0);
  static const Color shimmerHighlight = Color(0xFFF5F5F5);
}
